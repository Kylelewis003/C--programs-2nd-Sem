#include<stdio.h>
int main()
{
    int i;
    char str[100];
    printf("Enter string :\n");
    scanf("%s",str[i]);
    for(i = 0 ; str[i]!='\0' ; i++)
    {
        printf("%s",str[i]);
    }
    return 0;

}